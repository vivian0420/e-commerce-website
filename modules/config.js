const config = {
    app: {
        port: 8000,
        host: "localhost",
        name: "ecommerce"
    },
    db: {
        host: "localhost",
        port: 3306,
        name: "mongoDB"
    }
}

module.exports = config;